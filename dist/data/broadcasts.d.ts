/**
 * WNCORE ambient broadcast feed — rotating lore lines shown during runs.
 *
 * SPOILER RULE (from the bible — never violate): everything here is written
 * from a scared survivor's POV. Behavioral observations, in-world theories,
 * anomalies, factions. NEVER the real mechanism. No "veil", no "rejection",
 * no dimensional anything. The mystery is the point.
 */
export declare const BROADCAST_LINES: string[];
/** Rare intrusions — red, brief, wrong. Same voice as the menu title glitches. */
export declare const WHISPER_LINES: string[];
//# sourceMappingURL=broadcasts.d.ts.map